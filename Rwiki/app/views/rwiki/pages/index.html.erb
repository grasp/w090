<%= render "base" %>
<div id="pages">
  <div class="tools">
    <%= link_to(t("pages.wiki_index"), recent_pages_path )%>
    <% if can? :create, Rforum::Page %>
      <%= link_to(t("pages.create_page"), new_page_path, :class => "btn success")%>
    <% end %>
  </div>
  <%= raw Rforum::SiteConfig.wiki_index_html %>
</div>