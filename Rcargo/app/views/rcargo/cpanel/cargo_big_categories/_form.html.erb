<%= simple_form_for([:cpanel,@cpanel_cargo_big_category]) do |f| %>
  <% if @cpanel_cargo_big_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cpanel_cargo_big_category.errors.count, "error") %> prohibited this cpanel_cargo_big_category from being saved:</h2>

      <ul>
      <% @cpanel_cargo_big_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
