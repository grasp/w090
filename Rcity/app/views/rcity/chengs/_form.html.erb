<%= form_for(@cheng) do |f| %>
  <% if @cheng.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cheng.errors.count, "error") %> prohibited this cheng from being saved:</h2>

      <ul>
      <% @cheng.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
