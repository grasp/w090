<h1>Listing countries</h1>

<table>
  <tr>
    <th>Code</th>
    <th>Name</th>
    <th>Lat</th>
    <th>Lng</th>
    <th>Coordinates</th>
    <th>Loc</th>
    <th>Address</th>
    <th>nav</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @countries.each do |country| %>
  <tr>
    <td><%= country.code %></td>
    <td><%= country.name %></td>
    <td><%= country.lat %></td>
    <td><%= country.lng %></td>
    <td><%= country.coordinates %></td>
    <td><%= country.loc %></td>
    <td><%= country.address %></td>
     <td><%= link_to 'province nav', rcity.country_nav_path(country.code) %></td>
    <td><%= link_to 'Show', rcity.country_path(country) %></td>
    <td><%= link_to 'Edit', rcity.edit_country_path(country) %></td>
    <td><%= link_to 'Destroy', rcity.country_path(country), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Country', rcity.new_country_path %>
